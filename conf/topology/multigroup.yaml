# ./conf/topology/multigroup.yaml

# MULTIGROUP TOPOLOGY CONFIG:

defaults:
  - base # Inherit base topology template

_target_: src.flora.topology.MultiGroupTopology

# Required fields - must be overridden when using this config
groups: ??? # REQUIRED: List of CentralizedTopology configurations for each group
global_comm: ??? # REQUIRED: Communication config for inter-group/global connections
