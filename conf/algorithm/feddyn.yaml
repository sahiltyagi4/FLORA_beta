# FEDDYN ALGORITHM CONFIG:

defaults:
  - base # Inherit base algorithm template

_target_: src.flora.algorithms.FedDyn

# Algorithm-specific parameters
alpha: 0.1 # Controls strength of correction vector regularization
