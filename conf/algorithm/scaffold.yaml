# ------------------------------------------------------------------------------
# SCAFFOLD ALGORITHM CONFIG: SCAFFOLD algorithm with control variates
#
# ------------------------------------------------------------------------------

defaults:
  - base # Inherit base algorithm template

_target_: src.flora.algorithms.Scaffold

# No additional parameters - SCAFFOLD uses only the base template fields
