# FEDMOM ALGORITHM CONFIG:

defaults:
  - base # Inherit base algorithm template

_target_: src.flora.algorithms.FedMom

# Algorithm-specific parameters
momentum: 0.9 # Momentum coefficient for server-side velocity buffer
