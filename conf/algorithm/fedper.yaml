# FEDPER ALGORITHM CONFIG:

defaults:
  - base # Inherit base algorithm template

_target_: src.flora.algorithms.FedPer

# Algorithm-specific parameters
personal_layers: ["classifier", "head", "fc"] # Layer names to keep local (not aggregated)
