# FEDNOVA ALGORITHM CONFIG:

defaults:
  - base # Inherit base algorithm template

_target_: src.flora.algorithms.FedNova

# Algorithm-specific parameters
weight_decay: 0.0 # L2 regularization strength for local training
